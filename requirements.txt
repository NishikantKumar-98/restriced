# FastAPI Translation + OCR + Speech Recognition API
# Installation: pip install -r requirements.txt
#
# System Dependencies (install separately):
# - Tesseract OCR: Required for OCR functionality (see notes below)
# - ffmpeg: Required for speech recognition (see notes below)

# FastAPI Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Machine Learning / NLP
transformers>=4.35.0
torch>=2.0.0
huggingface-hub>=0.19.0
sentencepiece>=0.1.99
tokenizers>=0.15.0
protobuf>=4.24.0

# Image Processing / OCR
pillow>=10.0.0
pytesseract>=0.3.10
# Note: Tesseract OCR must be installed separately:
# - Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
# - Linux: sudo apt-get install tesseract-ocr tesseract-ocr-nep tesseract-ocr-sin
# - macOS: brew install tesseract tesseract-lang

# PDF Processing
pymupdf>=1.23.0

# Speech Recognition
openai-whisper>=20231117
# Note: ffmpeg must be installed separately for audio processing:
# - Windows: Download from https://ffmpeg.org/download.html
# - Linux: sudo apt-get install ffmpeg
# - macOS: brew install ffmpeg

# Additional dependencies
numpy>=1.24.0
requests>=2.31.0
accelerate>=0.24.0
safetensors>=0.4.0
